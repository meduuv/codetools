[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="codetools"
version="0.1.0"
description="Small source text transformation helpers"
requires-python=">=3.9"
license={text="MIT"}
[tool.setuptools.packages.find]
where=["src"]
